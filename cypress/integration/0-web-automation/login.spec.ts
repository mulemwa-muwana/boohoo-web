describe('Core front-end website', () => {

  // This will execute before every single test, we're just going to the baseURL.
  beforeEach(() => {
    cy.visit('/');
  })

  // This is a single test.
  it('can log in a user', () => {
    // Test steps here...
  })

})
