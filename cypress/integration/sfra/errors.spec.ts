describe('Errors', function () {

  // This will execute before every single test, we're just going to the baseURL.
  beforeEach(() => {

  });

  /**
     * Tests of out scope: None. (Feasability Analysis has not been performed).
     */

  it('TC001 - Authentication attempt with a non-existent email address will display the specific error message.', function () {

  });
  it('TC002 - Authentication attempt with an incorrect password will display the specific error message.', function () {

  });
});