describe('Localisation Subsystem', function () {

    // This will execute before every single test, we're just going to the baseURL.
    beforeEach(() => {

    })

    /**
     * Tests of out scope: None. (Feasability Analysis has not been performed).
     */

    it('TC001 - Verify that user can select any available locale from Country selector from footer area.', function () {

    })
    it('TC002 - Verify that changing URL of site redirects user to homepage of selected country.', function () {

    })
    it('TC003 - Verify that flags are matching elements with chosen country.', function () {

    })
    it('TC004 - Verify that currency and language selectors switch currency/language.', function () {
        
    })
})